# Database Configuration for Student Leave Management System
# MySQL Database Connection Properties

# Database connection settings
db.url=jdbc:mysql://localhost:3306/student_leave_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=trupti@123
db.driver=com.mysql.cj.jdbc.Driver

# Connection pool settings
db.pool.initialSize=5
db.pool.maxActive=20
db.pool.maxWait=10000
db.pool.testOnBorrow=true
db.pool.validationQuery=SELECT 1

# Application settings
app.name=Student Leave Management System
app.version=1.0.0
app.debug=true

# Security settings
security.password.minLength=6
security.session.timeout=3600

# Email settings (for future enhancement)
email.smtp.host=localhost
email.smtp.port=587
email.from=truptibhuskute@gmail.com
email.enabled=false

# File upload settings (for future enhancement)
upload.maxFileSize=5242880
upload.allowedTypes=pdf,doc,docx,jpg,jpeg,png
upload.directory=uploads/
